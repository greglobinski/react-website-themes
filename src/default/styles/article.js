import { css } from 'emotion';

const articleStyle = css`
  max-width: 600px;
  margin: 0 auto 30px;

  @keyframes entry {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }
`;

export default articleStyle;
